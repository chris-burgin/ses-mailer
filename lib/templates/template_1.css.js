const css = `
    main {
      font-family: Arial, Helvetica;
    }

    h1 {
      font-size: 45px;
    }

    h2 {
      font-size: 25px;
    }

    /* MAIN */
    main {
      max-width: 600px;
      margin: 0 auto;
      font-size: 16px;
    }

    /* HEADER */
    header h1 {
      margin-bottom: 0;
    }

    header h2 {
      font-size: 18px;
      font-weight: 400;
      margin-top: 0;
      margin-bottom: 0;
    }

    /* SECTION */
    section {
      margin-top: 20px;
    }

    /* FOOTER */
    footer {
      font-size: 12px;
      margin-top: 20px;
    }
  `

module.exports = css
